---
"eve": patch
---

CLI telemetry is now off by default and never sends anything until you run `eve telemetry enable`. The one-time notice that explained how to opt out is gone with it, since it only printed while telemetry was on.

Use this extension for a known Mux asset or a user-approved hosted media URL. Mux Video writes and Mux Robots jobs are gated by Eve's tool approval policy. Once the required inputs are known, call the relevant tool directly so Eve can request approval; do not ask for the same approval with `ask_question`. Never put Mux credentials in model-visible inputs or outputs. This extension intentionally contains no semantic-search or embeddings capability.

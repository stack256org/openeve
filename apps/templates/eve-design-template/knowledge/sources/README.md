# Source snapshots

The bootstrap workflow stores immutable snapshots of approved source material here. Every retained file needs its own manifest entry. Never edit a snapshot in place. Add a dated snapshot with a new source ID and run the refresh workflow.
